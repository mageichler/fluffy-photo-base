import { Link } from "react-router-dom";

function SearchBar() {
    
    return null
    {/* <form>
        <input type="text" placeholder="e.g. cat" value="keyword" onChange={(e) => setKeyword(e.target.value)}/>
        <Link type="submit" to="/results/" + >Fluff now!</Link>
    </form> */}
}

export default SearchBar;